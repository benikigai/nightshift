# Nightshift

Agentic development supervisor with a self-improving build loop and quality
backpressure.

[Product page](https://nightshift.kogenlabs.dev) |
[Original AgentForge demo](https://agent-forge-rho-ten.vercel.app)

This repo started as **AgentForge**, the Ralph Loop hackathon demo. It is now the
public proof run behind **Nightshift**: a supervisor for queued GitHub work that
routes builds through Graveyard, reviews through Handoff, and stops when
guardrails say stop.

The app is the receipt. It visualizes the autonomous build process that created
it: every feature, attempt, score, token count, commit, and failure recovery.

## The proof run

| Metric | Result |
| --- | ---: |
| Features | 29/30 passed |
| Average passing score | 9.69/10 |
| Iterations | 41 |
| Tokens | 109,270 |
| Cost | about $0.95 |
| Elapsed time | 76 minutes |

## How the loop works

```txt
Spec
  -> Graveyard build loop
  -> Codex implements one feature
  -> build/test gates run
  -> evaluator scores the result
  -> feedback retries or passes
  -> metrics are written
  -> git commits the accepted work
```

The important part is the quality gate. The builder does not grade itself. A
separate evaluator reviews the work, rejects weak attempts, and gives targeted
feedback before the loop tries again.

## Capabilities

- **Nightshift**: the supervisor that watches queued GitHub work and keeps an
  audit trail for every shift.
- **Graveyard**: the build loop that executes one scoped task at a time.
- **Handoff**: the review layer that decides approve, request changes, requeue,
  or escalate.
- **Guard**: budgets, rate limits, circuit breakers, permissions, and audit
  controls.

## From AgentForge to Nightshift

AgentForge proved that a build loop could make visible progress without a human
staring at every step. Nightshift turns that idea into a reusable system.

If you want the product story, start here:

```txt
https://nightshift.kogenlabs.dev
```

If you want the original dashboard proof, run this repo.

## Run locally

```sh
npm install
npm run dev
```

Then open:

```txt
http://localhost:3000
```

Build and test:

```sh
npm run build
npm test
```

## Key files

- `ralph-loop.sh` - outer loop that picks features, runs the builder, evaluates,
  commits, and moves on.
- `feature_list.json` - structured feature queue for the demo app.
- `evaluate.py` - evaluator bridge and scoring contract.
- `json_guard.py` - protects feature specs from being rewritten by the agent.
- `metrics_writer.py` - appends run data into `public/metrics.json`.
- `public/metrics.json` - the dashboard's source of truth.
- `src/` - the Next.js dashboard generated by the loop.

## Why star it

Star this if you are interested in agentic engineering loops that are:

- scoped to one task at a time
- reviewed by a separate evaluator
- backed by build/test gates
- observable through metrics and git history
- designed to stop, retry, or escalate instead of blindly continuing

This is not the final Nightshift product. It is the original working specimen.
